const AccountPage = () => {
  return (
    <div>
      <h1>{/* Account page will be set up with cloud firestore and watchlist array. */}</h1>;
    </div>
  );
};

export default AccountPage;


// The default security rules for test mode allow anyone with your database 
// reference to view, edit and delete all data in your database for the next 30 days